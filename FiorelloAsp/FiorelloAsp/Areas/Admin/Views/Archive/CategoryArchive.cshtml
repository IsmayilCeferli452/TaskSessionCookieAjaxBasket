@model IEnumerable<CategoryArchiveVM>

@{
    ViewData["Title"] = "Index";
}


<div class="container mt-5">
    <div class="table-responsive">
        <table class="table">
            <h1 class="text-center">
                Archive categories
            </h1>
            <div>
                <a class="btn btn-danger btn-sm mt-3 mb-3" asp-area="Admin" asp-controller="Category" asp-action="Create">
                    <span>Create category</span>
                    <i class="fa-solid fa-plus"></i>
                </a>
            </div>
            @foreach (var item in Model)
            {
                <thead>
                    <tr>
                        <th><b>Category</b></th>
                        <th><b>Created Date</b></th>
                        <th><b>Operations</b></th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="c-data">
                        <td>@item.CategoryName</td>
                        <td>@item.CreatedDate</td>
                        <td>
                            <form class="d-inline" method="post" asp-area="Admin" asp-controller="Category" asp-action="Delete" asp-route-id="@item.Id">
                                <button type="submit" class="btn btn-danger btn-sm"><i class="fa-solid fa-trash"></i></button>
                            </form>
                            <a class="btn btn-secondary btn-sm remove-archive" data-id="@item.Id">Restore</a>
                        </td>
                    </tr>
                </tbody>
            }
        </table>
    </div>
</div>

@section js {
    <script src="~/admin/assets/js/archive.js"></script>
}